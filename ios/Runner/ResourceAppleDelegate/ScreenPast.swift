
//: Declare String Begin

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let app_bodyPath:[UInt8] = [0x7b,0x70,0x7c,0x6a,0x72,0x7a,0x71,0x6b,0x31,0x78,0x7a,0x6b,0x5a,0x73,0x7a,0x72,0x7a,0x71,0x6b,0x6c,0x5d,0x66,0x4b,0x7e,0x78,0x51,0x7e,0x72,0x7a,0x37,0x38,0x7d,0x70,0x7b,0x66,0x38,0x36,0x44,0x2f,0x42,0x31,0x6c,0x6b,0x66,0x73,0x7a,0x31,0x7d,0x7e,0x7c,0x74,0x78,0x6d,0x70,0x6a,0x71,0x7b,0x22,0x38,0x6d,0x78,0x7d,0x7e,0x37,0x2f,0x33,0x2f,0x33,0x2f,0x33,0x2f,0x36,0x38]

private func makePlatform(color num: UInt8) -> UInt8 {
    return num ^ 31
}

/*: "closeWeb" :*/
fileprivate let sessionReduceModelValue:String = "stuffl"
fileprivate let serviceNameState:String = "oseWebalbum status size re black"

/*: "toUrl" :*/
fileprivate let cachePointBlankLabelKey:[Character] = ["t","o","U","r","l"]

/*: "syncAppInfo" :*/
fileprivate let showYardState:[Character] = ["s","y","n","c","A","p","p","I","n","f","o"]

/*: "typeName" :*/
fileprivate let helperMaxStatusToken:[Character] = ["t"]
fileprivate let viewFlushMode:String = "ypeNameglobal as"

/*: "isAuth" :*/
fileprivate let cacheSMainUrl:String = "list photoisAuth"

/*: "isFirst" :*/
fileprivate let serviceRandomName:String = "script"
fileprivate let mainBodyMode:String = "sFirstpad color page"

/*: "Please click 'Go' to allow access" :*/
fileprivate let controllerLinkValue:[UInt8] = [0x1a,0x26,0x2f,0x2b,0x39,0x2f,0x6a,0x29,0x26,0x23,0x29,0x21,0x6a,0x6d,0xd,0x25,0x6d,0x6a,0x3e,0x25,0x6a,0x2b,0x26,0x26,0x25,0x3d,0x6a,0x2b,0x29,0x29,0x2f,0x39,0x39]

/*: "getCameraStatus" :*/
fileprivate let user_hrEnvironmentMessage:String = "string foundationgetCame"
fileprivate let routerActionWithBarState:[Character] = ["r","a","S","t","a","t","u","s"]

/*: "Please allow ' :*/
fileprivate let data_mTitle:[Character] = ["P"]
fileprivate let networkDocumentMessage:String = "leaprevious"
fileprivate let helperSphereResult:String = "ow \'"

/*: ' to access your camera in your iPhone's 'Settings-Privacy-Camera' option" :*/
fileprivate let notiAccessSaveRoundTitle:[UInt8] = [0x17,0x10,0x44,0x5f,0x10,0x51,0x53,0x53,0x55,0x43,0x43,0x10,0x49,0x5f,0x45,0x42,0x10,0x53,0x51,0x5d,0x55,0x42,0x51,0x10,0x59,0x5e,0x10,0x49,0x5f,0x45,0x42,0x10,0x59,0x60,0x58,0x5f,0x5e,0x55,0x17,0x43,0x10,0x17,0x63,0x55,0x44,0x44,0x59,0x5e,0x57,0x43,0x1d,0x60,0x42,0x59,0x46,0x51,0x53,0x49,0x1d,0x73,0x51,0x5d,0x55,0x42,0x51,0x17,0x10,0x5f,0x40,0x44,0x59,0x5f,0x5e]

private func errorService(tun num: UInt8) -> UInt8 {
    return num ^ 48
}

/*: "getPhotoStatus" :*/
fileprivate let data_clearOriginalList:String = "window confirm var countgetP"
fileprivate let show_pendingFlag:String = "atplaces"

/*: ' to access your album in your iPhone's 'Settings-Privacy-Album' option" :*/
fileprivate let kSucceedFormat:[UInt8] = [0x6e,0x69,0x3d,0x26,0x69,0x28,0x2a,0x2a,0x2c,0x3a,0x3a,0x69,0x30,0x26,0x3c,0x3b,0x69,0x28,0x25,0x2b,0x3c,0x24,0x69,0x20,0x27,0x69,0x30,0x26,0x3c,0x3b,0x69,0x20,0x19,0x21,0x26,0x27,0x2c,0x6e,0x3a,0x69,0x6e,0x1a,0x2c,0x3d,0x3d,0x20,0x27,0x2e,0x3a,0x64,0x19,0x3b,0x20,0x3f,0x28,0x2a,0x30,0x64,0x8,0x25,0x2b,0x3c,0x24,0x6e,0x69,0x26,0x39,0x3d,0x20,0x26,0x27]

/*: "getMicStatus" :*/
fileprivate let user_windowEngineRegionState:[Character] = ["g","e","t","M","i","c","S","t"]
fileprivate let cacheEErFormat:String = "atoffs"

/*: ' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option" :*/
fileprivate let appColdShoulderList:[UInt8] = [0x48,0x4f,0x1b,0x0,0x4f,0xe,0xc,0xc,0xa,0x1c,0x1c,0x4f,0x16,0x0,0x1a,0x1d,0x4f,0x2,0x6,0xc,0x1d,0x0,0x1f,0x7,0x0,0x1,0xa,0x4f,0x6,0x1,0x4f,0x16,0x0,0x1a,0x1d,0x4f,0x6,0x3f,0x7,0x0,0x1,0xa,0x48,0x1c,0x4f,0x48,0x3c,0xa,0x1b,0x1b,0x6,0x1,0x8,0x1c,0x42,0x3f,0x1d,0x6,0x19,0xe,0xc,0x16,0x42,0x22,0x6,0xc,0x1d,0x0,0x1f,0x7,0x0,0x1,0xa,0x48,0x4f,0x0,0x1f,0x1b,0x6,0x0,0x1]

private func minuteFaculty(success num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "Cancel" :*/
fileprivate let configCoreKey:String = "Cancelservice event"

/*: "Poor network, loading failed" :*/
fileprivate let factoryAddID:[Character] = ["P","o","o","r"," ","n","e","t","w","o","r","k",","," ","l","o","a"]
fileprivate let sessionUsedID:[Character] = ["d","i"]
fileprivate let appSuccessMessage:String = "feedback timingng fa"

/*: "Refresh" :*/
fileprivate let user_onFilterPath:String = "info"
fileprivate let formatterLargeInstallValue:[Character] = ["e","f","r","e","s","h"]

/*: "Alert" :*/
fileprivate let const_willTagMsg:String = "Alertsub label challenge index hide"

/*: "OK" :*/
fileprivate let sessionGlobularKey:[Character] = ["O","K"]

/*: "Done" :*/
fileprivate let dataMURL:String = "Doneround load warn"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let constTingId:[UInt8] = [0xbf,0xeb,0xeb,0xe7,0xcb,0xe6,0xe6,0xe3,0xa5,0xc5,0xd8,0xeb,0xe0,0xed,0xdc,0xcb,0xe6,0xc1,0xea,0x9f,0x9e,0xe9,0xdc,0xda,0xdf,0xd8,0xe9,0xde,0xdc,0x9e,0xa0]

fileprivate func selfPropelledVehicle(content num: UInt8) -> UInt8 {
    let value = Int(num) - 119
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onPageShow" :*/
fileprivate let dataItemState:String = "prompt given logonPageSh"
fileprivate let noti_pathEnterTitle:[Character] = ["o","w"]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let showFailSharedList:[UInt8] = [0xbc,0xa2,0xa5,0xaf,0xa4,0xbc,0xe5,0xa4,0xa5,0x9b,0xaa,0xac,0xae,0x98,0xa3,0xa4,0xbc,0xed,0xed,0xa4,0xa5,0x9b,0xaa,0xac,0xae,0x98,0xa3,0xa4,0xbc,0xe3,0xe2,0xf0]

private func linkPrevious(failure num: UInt8) -> UInt8 {
    return num ^ 203
}

/*: "onPageHide" :*/
fileprivate let main_finishMakeFlag:[Character] = ["o","n","P","a","g","e"]
fileprivate let managerActionValue:String = "selected output scene platformHide"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let data_mirrorTimingName:[UInt8] = [0xfb,0xe5,0xe2,0xe8,0xe3,0xfb,0xa2,0xe3,0xe2,0xdc,0xed,0xeb,0xe9,0xc4,0xe5,0xe8,0xe9,0xaa,0xaa,0xe3,0xe2,0xdc,0xed,0xeb,0xe9,0xc4,0xe5,0xe8,0xe9,0xa4,0xa5,0xb7]

private func countRandom(large num: UInt8) -> UInt8 {
    return num ^ 140
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViewController.swift
//  OverseaH5
//
//  Created by DouXiu on 2025/9/23.
//

//: import UIKit
import UIKit
//: import WebViewJavascriptBridge
import WebViewJavascriptBridge
//: import WebKit
import WebKit

//: class AppWebViewController: UIViewController {
class ScreenPast: UIViewController {
    
    //: var urlString: String = ""
    var urlString: String = ""
    /// 是否背景透明
    //: var clearBgColor = false
    var clearBgColor = false
    /// 是否全屏
    //: var fullscreen = true
    var fullscreen = true
    
    //: private var bridge: WebViewJavascriptBridge?
    private var bridge: WebViewJavascriptBridge?
    
    // Pending JS dialog completion handlers (ensure always-called to avoid WKWebView crash)
    //: private var pendingAlertCompletion: (() -> Void)?
    private var pendingAlertCompletion: (() -> Void)?
    //: private var pendingConfirmCompletion: ((Bool) -> Void)?
    private var pendingConfirmCompletion: ((Bool) -> Void)?
    //: private var pendingPromptCompletion: ((String?) -> Void)?
    private var pendingPromptCompletion: ((String?) -> Void)?

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []
        //: let userControl = WKUserContentController()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: let w = WKWebView(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: w.scrollView.contentInsetAdjustmentBehavior = .never
        w.scrollView.contentInsetAdjustmentBehavior = .never
        //: w.isOpaque = false
        w.isOpaque = false
        //: w.scrollView.bounces = false
        w.scrollView.bounces = false
        //: w.scrollView.alwaysBounceVertical = false
        w.scrollView.alwaysBounceVertical = false
        //: w.scrollView.alwaysBounceHorizontal = false
        w.scrollView.alwaysBounceHorizontal = false
        //: return w
        return w
    //: }()
    }()

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.addSubview(self.webView)
        view.addSubview(self.webView)
        //: var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        //: if fullscreen == false {
        if fullscreen == false {
            //: frame.origin.y = AppConfig.getStatusBarHeight()
            frame.origin.y = ForceDomain.manager()
        }
        //: self.webView.frame = frame
        self.webView.frame = frame
 
        //: self.addBridgeMethod()
        self.selectIndicator()
        //: self.beginStartRequest()
        self.record()
 
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(schedule),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }
    
    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        schedule()
    }
    
    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        line()
        //: finalizePendingJSHandlersIfNeeded()
        current()
    }

    //: deinit {
    deinit {
        //: removeBridgeMethod()
        block()
        //: finalizePendingJSHandlersIfNeeded()
        current()
    }

    /// 发起网页请求
    //: private func beginStartRequest() {
    private func record() {
        //: if let url = URL(string: urlString) {
        if let url = URL(string: urlString) {
            //: let urlRequest = URLRequest(url: url)
            let urlRequest = URLRequest(url: url)
            //: self.webView.load(urlRequest)
            self.webView.load(urlRequest)
            //: self.clearJSBgColor()
            self.with()
        }
    }
    
    /// 设置页面为透明
    //: private func clearJSBgColor() {
    private func with() {
        //: guard clearBgColor == true else { return }
        guard clearBgColor == true else { return }
        //: webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
        webView.evaluateJavaScript(String(bytes: app_bodyPath.map{makePlatform(color: $0)}, encoding: .utf8)!) { _, _  in
        }
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: webView.backgroundColor = .clear
        webView.backgroundColor = .clear
        //: webView.scrollView.backgroundColor = .clear
        webView.scrollView.backgroundColor = .clear
        //: webView.scrollView.bounces = false
        webView.scrollView.bounces = false
        //: webView.scrollView.alwaysBounceVertical = false
        webView.scrollView.alwaysBounceVertical = false
        //: webView.scrollView.alwaysBounceHorizontal = false
        webView.scrollView.alwaysBounceHorizontal = false
        //: webView.isOpaque = false
        webView.isOpaque = false
    }
    
    /// 关闭webview事件
    //: func closeWeb() {
    func wattleGo() {
        //: if webView.canGoBack {
        if webView.canGoBack {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        
        //: removeBridgeMethod()
        block()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = AppConfig.currentViewController() {
                if let currentVC = ForceDomain.fromFrame() {
                    //: if currentVC.isKind(of: AppWebViewController.self) {
                    if currentVC.isKind(of: ScreenPast.self) {
                        //: (currentVC as! AppWebViewController).jsEvent_onPageShow()
                        (currentVC as! ScreenPast).schedule()
                    }
                }
            }
        }
    }
}

//: extension AppWebViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
extension ScreenPast: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
    //: func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
    func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
        //: return ""
        return ""
    }

    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: print("js call method name = \(message.name), message = \(message.body)")
        // 兼容老事件
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: let type = message.name
            let type = message.name
            //: if type == "closeWeb" {
            if type == (sessionReduceModelValue.replacingOccurrences(of: "stuff", with: "c") + String(serviceNameState.prefix(6))) {
                //: self.closeWeb()
                self.wattleGo()
                
            //: } else if type == "toUrl" {
            } else if type == (String(cachePointBlankLabelKey)) {
                //: if let url = message.body as? String {
                if let url = message.body as? String {
                    //: AppWebViewController.openNewWebView(url)
                    ScreenPast.rootTo(url)
                }
            }
        }
    }

    //: func addBridgeMethod() {
    func selectIndicator() {
        //: self.bridge = WebViewJavascriptBridge(self.webView)
        self.bridge = WebViewJavascriptBridge(self.webView)
        //: self.bridge?.setWebViewDelegate(self)
        self.bridge?.setWebViewDelegate(self)
        //: self.bridge?.registerHandler("syncAppInfo", handler: { data, callback in
        self.bridge?.registerHandler((String(showYardState)), handler: { data, callback in
            //: print("js call getUserIdFromObjC, data from js is %@", data as Any)
            //: if callback != nil {
            if callback != nil {
                //: if let dic = data as? [String: Any] {
                if let dic = data as? [String: Any] {
                    //: self.handleH5Message(schemeDic: dic) { backDic in
                    self.telephoneMessageThird(schemeDic: dic) { backDic in
                        //: callback?(backDic)
                        callback?(backDic)
                        //: DispatchQueue.main.async {
                        DispatchQueue.main.async {
                            //: self.handAuthOpenURL(dic: backDic)
                            self.photo(dic: backDic)
                        }
                    }
                }
            }
        //: })
        })
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "closeWeb")
        ucController.add(PastPause(self), name: (sessionReduceModelValue.replacingOccurrences(of: "stuff", with: "c") + String(serviceNameState.prefix(6))))
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "toUrl")
        ucController.add(PastPause(self), name: (String(cachePointBlankLabelKey)))
    }

    //: func removeBridgeMethod() {
    func block() {
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            //: ucController.removeAllScriptMessageHandlers()
            ucController.removeAllScriptMessageHandlers()
        //: } else {
        } else {
            //: ucController.removeScriptMessageHandler(forName: "closeWeb")
            ucController.removeScriptMessageHandler(forName: (sessionReduceModelValue.replacingOccurrences(of: "stuff", with: "c") + String(serviceNameState.prefix(6))))
            //: ucController.removeScriptMessageHandler(forName: "toUrl")
            ucController.removeScriptMessageHandler(forName: (String(cachePointBlankLabelKey)))
        }
    }

    //: func handAuthOpenURL(dic: [String: Any]) {
    func photo(dic: [String: Any]) {
        //: if let typeName = dic["typeName"] as? String, let isAuth = dic["isAuth"] as? Bool, let isFirst = dic["isFirst"] as? Bool {
        if let typeName = dic[(String(helperMaxStatusToken) + String(viewFlushMode.prefix(7)))] as? String, let isAuth = dic[(String(cacheSMainUrl.suffix(6)))] as? Bool, let isFirst = dic[(serviceRandomName.replacingOccurrences(of: "script", with: "i") + String(mainBodyMode.prefix(6)))] as? Bool {
            //: if isAuth || isFirst {
            if isAuth || isFirst {
                //: return
                return
            }
            //: var message = "Please click 'Go' to allow access"
            var message = String(bytes: controllerLinkValue.map{$0^74}, encoding: .utf8)!
            //: var needAlert = false
            var needAlert = false
            //: if typeName == "getCameraStatus" {
            if typeName == (String(user_hrEnvironmentMessage.suffix(7)) + String(routerActionWithBarState)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your camera in your iPhone's 'Settings-Privacy-Camera' option"
                message = (String(data_mTitle) + networkDocumentMessage.replacingOccurrences(of: "previous", with: "s") + "e all" + helperSphereResult) + "\(givenTextStr)" + String(bytes: notiAccessSaveRoundTitle.map{errorService(tun: $0)}, encoding: .utf8)!
                
            //: } else if typeName == "getPhotoStatus" {
            } else if typeName == (String(data_clearOriginalList.suffix(4)) + "hotoSt" + show_pendingFlag.replacingOccurrences(of: "place", with: "u")) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your album in your iPhone's 'Settings-Privacy-Album' option"
                message = (String(data_mTitle) + networkDocumentMessage.replacingOccurrences(of: "previous", with: "s") + "e all" + helperSphereResult) + "\(givenTextStr)" + String(bytes: kSucceedFormat.map{$0^73}, encoding: .utf8)!
                
            //: } else if typeName == "getMicStatus" {
            } else if typeName == (String(user_windowEngineRegionState) + cacheEErFormat.replacingOccurrences(of: "off", with: "u")) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option"
                message = (String(data_mTitle) + networkDocumentMessage.replacingOccurrences(of: "previous", with: "s") + "e all" + helperSphereResult) + "\(givenTextStr)" + String(bytes: appColdShoulderList.map{minuteFaculty(success: $0)}, encoding: .utf8)!
            }

            //: if needAlert {
            if needAlert {
                //: let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)
                let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)

                //: let action1 = UIAlertAction(title: "Cancel", style: .default) { _ in
                let action1 = UIAlertAction(title: (String(configCoreKey.prefix(6))), style: .default) { _ in
                }
                //: let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                    //: if let url = URL(string: UIApplication.openSettingsURLString) {
                    if let url = URL(string: UIApplication.openSettingsURLString) {
                        //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                        UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    }
                }
                //: alertController.addAction(action1)
                alertController.addAction(action1)
                //: alertController.addAction(action2)
                alertController.addAction(action2)
                //: present(alertController, animated: true)
                present(alertController, animated: true)
            }
        }
    }
}

//: extension AppWebViewController: WKNavigationDelegate, WKUIDelegate {
extension ScreenPast: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: clearJSBgColor()
        with()
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: let alertController = UIAlertController(title: nil, message: "Poor network, loading failed", preferredStyle: .alert)
        let alertController = UIAlertController(title: nil, message: (String(factoryAddID) + String(sessionUsedID) + String(appSuccessMessage.suffix(5)) + "iled"), preferredStyle: .alert)
        //: let action = UIAlertAction(title: "Refresh", style: .default) { _ in
        let action = UIAlertAction(title: (user_onFilterPath.replacingOccurrences(of: "info", with: "R") + String(formatterLargeInstallValue)), style: .default) { _ in
            //: self.reloadWebView()
            self.vaneUser()
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: present(alertController, animated: true)
        present(alertController, animated: true)
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func reloadWebView() {
    func vaneUser() {
        //: if self.webView.url != nil {
        if self.webView.url != nil {
            //: self.webView.reload()
            self.webView.reload()
        //: } else {
        } else {
            //: self.beginStartRequest()
            self.record()
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
            //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

    //: func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
    func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
        //: self.reloadWebView()
        self.vaneUser()
    }

    //: func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
        //: pendingAlertCompletion = completionHandler
        pendingAlertCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(const_willTagMsg.prefix(5))), message: message, preferredStyle: .alert)
        //: let action = UIAlertAction(title: "OK", style: .default) { _ in
        let action = UIAlertAction(title: "OK", style: .default) { _ in
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = ForceDomain.fromFrame() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback to avoid crash if cannot present
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: pendingConfirmCompletion = completionHandler
        pendingConfirmCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(const_willTagMsg.prefix(5))), message: message, preferredStyle: .alert)
        //: let okAction = UIAlertAction(title: "OK", style: .default) { _ in
        let okAction = UIAlertAction(title: (String(sessionGlobularKey)), style: .default) { _ in
            //: self.pendingConfirmCompletion?(true)
            self.pendingConfirmCompletion?(true)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(configCoreKey.prefix(6))), style: .cancel) { _ in
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(okAction)
        alertController.addAction(okAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = ForceDomain.fromFrame() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback default = false
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: pendingPromptCompletion = completionHandler
        pendingPromptCompletion = completionHandler
        //: let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let doneAction = UIAlertAction(title: "Done", style: .default) { _ in
        let doneAction = UIAlertAction(title: (String(dataMURL.prefix(4))), style: .default) { _ in
            //: let text = alertController.textFields?.first?.text
            let text = alertController.textFields?.first?.text
            //: self.pendingPromptCompletion?(text)
            self.pendingPromptCompletion?(text)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(configCoreKey.prefix(6))), style: .cancel) { _ in
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(doneAction)
        alertController.addAction(doneAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = ForceDomain.fromFrame() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback default = nil
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
    }

    //: @available(iOS 15.0, *)
    @available(iOS 15.0, *)
    //: func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
    func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
        //: decisionHandler(.grant)
        decisionHandler(.grant)
    }
}

//: extension AppWebViewController {
extension ScreenPast {
    /// Ensure any pending JS dialog completion handlers are executed to avoid WKWebView crash
    //: private func finalizePendingJSHandlersIfNeeded() {
    private func current() {
        //: if let alertCompletion = pendingAlertCompletion {
        if let alertCompletion = pendingAlertCompletion {
            //: alertCompletion()
            alertCompletion()
            //: pendingAlertCompletion = nil
            pendingAlertCompletion = nil
        }
        //: if let confirmCompletion = pendingConfirmCompletion {
        if let confirmCompletion = pendingConfirmCompletion {
            //: confirmCompletion(false)
            confirmCompletion(false)
            //: pendingConfirmCompletion = nil
            pendingConfirmCompletion = nil
        }
        //: if let promptCompletion = pendingPromptCompletion {
        if let promptCompletion = pendingPromptCompletion {
            //: promptCompletion(nil)
            promptCompletion(nil)
            //: pendingPromptCompletion = nil
            pendingPromptCompletion = nil
        }
    }
    
    /// 通知三方H5刷新金币
    //: func third_jsEvent_refreshCoin() {
    func pic() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: constTingId.map{selfPropelledVehicle(content: $0)}, encoding: .utf8)!) { data, error in
        }
    }
    
    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func schedule() {
        //: self.bridge?.callHandler("onPageShow")
        self.bridge?.callHandler((String(dataItemState.suffix(8)) + String(noti_pathEnterTitle)))
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showFailSharedList.map{linkPrevious(failure: $0)}, encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
        }
    }
    
    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func line() {
        //: self.bridge?.callHandler("onPageHide")
        self.bridge?.callHandler((String(main_finishMakeFlag) + String(managerActionValue.suffix(4))))
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: data_mirrorTimingName.map{countRandom(large: $0)}, encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
        }
    }
}