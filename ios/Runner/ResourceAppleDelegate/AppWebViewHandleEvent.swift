
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let controllerUserPreviousScriptPath:String = "getDpath kit"
fileprivate let managerProductDict:String = "eviceIDprivacy element faculty pending"

/*: "getFirebaseID" :*/
fileprivate let factorySecondId:String = "getFiauto content secret self day"

/*: "getAreaISO" :*/
fileprivate let const_regionHidePicUrl:String = "part policy empty netgetA"

/*: "getProxyStatus" :*/
fileprivate let factoryOriginLinkBarKey:String = "getProxySsub hour where"
fileprivate let notiDisappearDateStatus:String = "modeltus"

/*: "getMicStatus" :*/
fileprivate let serviceThatValue:String = "getMion base"
fileprivate let notiGoStr:String = "urun"

/*: "getPhotoStatus" :*/
fileprivate let kPrivacyTitle:[Character] = ["g","e","t","P","h","o","t","o","S","t","a","t","u"]
fileprivate let serviceStyleHandleDict:String = "that"

/*: "getCameraStatus" :*/
fileprivate let controllerLabStatus:[Character] = ["g","e","t","C","a","m"]
fileprivate let routerIndexTitle:String = "eraStatusstring return"

/*: "reportAdjust" :*/
fileprivate let viewStatusAfterValue:[Character] = ["r","e","p","o","r","t","A","d","j","u"]
fileprivate let networkFailResult:String = "srecord"

/*: "requestLocalPush" :*/
fileprivate let app_headCount:[Character] = ["r","e","q","u","e","s","t"]
fileprivate let sessionPointMethodList:[Character] = ["L","o","c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let notiScreenId:String = "getLrequest substance"
fileprivate let app_elementPadStr:[Character] = ["e"]

/*: "getTimeZone" :*/
fileprivate let appChallengeMode:[Character] = ["g","e","t","T","i","m","e","Z","o","n","e"]

/*: "getInstalledApps" :*/
fileprivate let controllerEngineToMessage:String = "getImessage re please case ad"
fileprivate let failureAdjustmentPath:String = "country request take service disappearedApps"

/*: "getSystemUUID" :*/
fileprivate let kPleaseID:String = "getSyappear wait no disk"
fileprivate let loggerStrideKey:[Character] = ["s","t","e","m","U","U","I","D"]

/*: "getCountryCode" :*/
fileprivate let formatterKindResult:[Character] = ["g","e","t","C","o","u","n","t","r","y","C","o","d"]
fileprivate let engineAdmonishProcessValue:[Character] = ["e"]

/*: "inAppRating" :*/
fileprivate let dataAreaValue:String = "disabled on body kitinAppR"
fileprivate let loggerApplicationMapStr:String = "environmenting"

/*: "apPay" :*/
fileprivate let managerLaterName:String = "apPayoriginal sound prompt background"

/*: "subscribe" :*/
fileprivate let const_afterSecret:[Character] = ["s","u","b","s","c"]
fileprivate let managerInputMinuteFormat:[Character] = ["r","i","b","e"]

/*: "openSystemBrowser" :*/
fileprivate let k_moveReduceStatus:[Character] = ["o","p","e","n","S","y","s","t","e","m","B","r","o","w","s","e","r"]

/*: "closeWebview" :*/
fileprivate let modelSelectedFlag:[Character] = ["c","l","o","s","e","W","e","b","v","i"]
fileprivate let routerPhotoManagerStatus:[Character] = ["e","w"]

/*: "openNewWebview" :*/
fileprivate let routerPlatformCount:String = "opwithn"
fileprivate let loggerPleaseName:String = "bvtelephonew"

/*: "reloadWebview" :*/
fileprivate let noti_hourCount:String = "reloatun"
fileprivate let formatterAdUrl:String = "black with click capture searchWebview"

/*: "openSettings" :*/
fileprivate let app_netName:String = "openSettpath filter language link name"
fileprivate let sessionPunchValue:String = "istartgs"

/*: "getNotificationStatus" :*/
fileprivate let parserOffTimingTriggerMessage:[Character] = ["g","e","t","N","o","t","i","f","i","c","a","t","i","o","n","S","t","a","t"]
fileprivate let helperOrdinalKey:String = "US"

/*: "setScheduledLocalPush" :*/
fileprivate let managerDisabledString:String = "sapp"
fileprivate let managerValidVersion:String = "field visible bounce format observertSched"
fileprivate let data_elementMode:String = "ocaforward"

/*: "typeName" :*/
fileprivate let modelDayStatus:[UInt8] = [0x61,0x6c,0x65,0x70,0x5b,0x74,0x78,0x70]

private func coreGroup(version num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "deviceID" :*/
fileprivate let notiNetValue:[UInt8] = [0xbe,0xbf,0xac,0xb3,0xb9,0xbf,0x93,0x9e]

private func offceMore(agent num: UInt8) -> UInt8 {
    return num ^ 218
}

/*: "fireBaseID" :*/
fileprivate let kCaptureMode:[UInt8] = [0x81,0x8e,0x95,0x82,0xa5,0x86,0x94,0x82,0xae,0xa3]

private func ofTowardSelect(format num: UInt8) -> UInt8 {
    return num ^ 231
}

/*: "areaISO" :*/
fileprivate let app_fireBehaviorResult:[UInt8] = [0x4f,0x53,0x49,0x61,0x65,0x72,0x61]

/*: "isProxy" :*/
fileprivate let k_rateDict:[UInt8] = [0xbf,0xa5,0x86,0xa4,0xb9,0xae,0xaf]

private func productionDisplay(confirm num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "langCode" :*/
fileprivate let modelDecideMessage:[UInt8] = [0x9e,0x93,0xa0,0x99,0x75,0xa1,0x96,0x97]

fileprivate func timeServer(stop num: UInt8) -> UInt8 {
    let value = Int(num) + 206
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "timeZone" :*/
fileprivate let dataDigitiserID:[UInt8] = [0x4a,0x3f,0x43,0x3b,0x30,0x45,0x44,0x3b]

fileprivate func administrativeDivision(global num: UInt8) -> UInt8 {
    let value = Int(num) + 42
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "installedApps" :*/
fileprivate let loggerYourValue:[UInt8] = [0x73,0x70,0x70,0x41,0x64,0x65,0x6c,0x6c,0x61,0x74,0x73,0x6e,0x69]

/*: "systemUUID" :*/
fileprivate let showBarrelStatus:[UInt8] = [0x43,0x49,0x43,0x44,0x55,0x5d,0x65,0x65,0x79,0x74]

/*: "countryCode" :*/
fileprivate let sessionZoneApplicationValue:[UInt8] = [0x3b,0x37,0x2d,0x36,0x2c,0x2a,0x21,0x1b,0x37,0x3c,0x3d]

/*: "status" :*/
fileprivate let appHeadFlag:[UInt8] = [0xc8,0xcf,0xda,0xcf,0xce,0xc8]

private func sumCenter(flexible num: UInt8) -> UInt8 {
    return num ^ 187
}

/*: "isAuth" :*/
fileprivate let data_privacyColorTowardName:[UInt8] = [0x2e,0x34,0x6,0x32,0x33,0x2f]

/*: "isFirst" :*/
fileprivate let networkDisplayMessage:[UInt8] = [0x81,0x9b,0xae,0x81,0x9a,0x9b,0x9c]

private func errorFailure(product num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: __LocalPush" :*/
fileprivate let noti_arrayPath:String = "__Locakey stop max pad"

/*: "identifier" :*/
fileprivate let networkErrorKey:[UInt8] = [0xb,0x6,0x7,0xc,0x16,0xb,0x4,0xb,0x7,0x10]

private func goOriginal(finish num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "HTTPProxy" :*/
fileprivate let routerEnterSecret:[Character] = ["H","T","T","P","P","r","o"]
fileprivate let noti_discourtesyActivityName:String = "xphoto"

/*: "HTTPSProxy" :*/
fileprivate let sessionBasisState:String = "HcontrolPS"
fileprivate let modelMakeStr:[Character] = ["P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let managerSubGlobalDate:String = "SOpresentationKS"

/*: "__SCOPED__" :*/
fileprivate let kGrantStatusSecret:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let const_showPath:String = "toriginp"

/*: "tun" :*/
fileprivate let serviceSearchData:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let sessionStatusMigrationStr:String = "ifilesec"

/*: "ppp" :*/
fileprivate let cacheArrayStatus:String = "buildp"

/*: "Retry After or Go to " :*/
fileprivate let constContactWhiteMessage:String = "Retryappear toward"
fileprivate let kTransformURL:[Character] = [" ","A","f","t","e","r"," ","o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let appHourMessage:[Character] = ["F","e","e"]
fileprivate let dataThirdList:String = "dpackageck"

/*: " to contact us" :*/
fileprivate let kStateFrameValue:[Character] = [" ","t","o"," ","c","o","n","t","a","c"]
fileprivate let sessionTitleUrl:String = "as contact foundation build nott us"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let viewOpengoingPackageMigrationSecret     = (String(controllerUserPreviousScriptPath.prefix(4)) + String(managerProductDict.prefix(7)))        // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let parserMicIndexPath   = (String(factorySecondId.prefix(5)) + "rebaseID")      // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let main_partToLogUrl      = (String(const_regionHidePicUrl.suffix(4)) + "reaISO")         // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let engineInstallValue  = (String(factoryOriginLinkBarKey.prefix(9)) + notiDisappearDateStatus.replacingOccurrences(of: "model", with: "ta"))     // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let app_trustStuffState    = (String(serviceThatValue.prefix(5)) + "cStat" + notiGoStr.replacingOccurrences(of: "run", with: "s"))       // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let showExceptionStr  = (String(kPrivacyTitle) + serviceStyleHandleDict.replacingOccurrences(of: "that", with: "s"))     // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let data_countMsg = (String(controllerLabStatus) + String(routerIndexTitle.prefix(9)))    // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let noti_viewMsg    = (String(viewStatusAfterValue) + networkFailResult.replacingOccurrences(of: "record", with: "t"))       // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let modelAppValue = (String(app_headCount) + String(sessionPointMethodList))  // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let engineRandomMessage      = (String(notiScreenId.prefix(4)) + "angCod" + String(app_elementPadStr))        // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let engineCanulateName      = (String(appChallengeMode))        // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let viewExceptionVersion = (String(controllerEngineToMessage.prefix(4)) + "nstall" + String(failureAdjustmentPath.suffix(6)))   // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let appLocalVersion    = (String(kPleaseID.prefix(5)) + String(loggerStrideKey))      // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let configPlacePushMode   = (String(formatterKindResult) + String(engineAdmonishProcessValue))     // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let cacheDoinggoingStr      = (String(dataAreaValue.suffix(6)) + loggerApplicationMapStr.replacingOccurrences(of: "environment", with: "at"))        // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let data_toMsg            = (String(managerLaterName.prefix(5)))              // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let loggerLocalValue        = (String(const_afterSecret) + String(managerInputMinuteFormat))          // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let cacheSphereName = (String(k_moveReduceStatus)) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let kOutputPath     = (String(modelSelectedFlag) + String(routerPhotoManagerStatus))       // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let main_levelId   = (routerPlatformCount.replacingOccurrences(of: "with", with: "e") + "NewWe" + loggerPleaseName.replacingOccurrences(of: "telephone", with: "ie"))     // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let cacheServerMessage    = (noti_hourCount.replacingOccurrences(of: "tun", with: "d") + String(formatterAdUrl.suffix(7)))      // 重载webView
//: private let openSettings = "openSettings"                   // v2.0.4新增：打开通知设置页
private let main_installTitle = (String(app_netName.prefix(8)) + sessionPunchValue.replacingOccurrences(of: "start", with: "n"))                   // v2.0.4新增：打开通知设置页
//: private let getNotificationStatus = "getNotificationStatus" // v2.0.4新增：获取通知权限
private let modelResourceFormat = (String(parserOffTimingTriggerMessage) + helperOrdinalKey.lowercased()) // v2.0.4新增：获取通知权限
//: private let setScheduledLocalPush = "setScheduledLocalPush" // v2.0.5新增：设置定时本地消息推送
private let main_photoFlag = (managerDisabledString.replacingOccurrences(of: "app", with: "e") + String(managerValidVersion.suffix(6)) + "uledL" + data_elementMode.replacingOccurrences(of: "forward", with: "l") + "Push") // v2.0.5新增：设置定时本地消息推送

//: struct JSMessageModel: HandyJSON {
struct FlagSort: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: SchemeLoopDevice?
    // 内购/订阅 H5参数
    //: var goodsId: String?     
    var goodsId: String?     // 商品Id
    //: var source: Int?         
    var source: Int?         // 充值来源
    //: var type: Int?           
    var type: Int?           // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?         
    var url: String?         // url
    //: var fullscreen: Int?     
    var fullscreen: Int?     // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?   
    var transparency: Int?   // transparency：0-webview白色背景 1-webview透明背景
    //: var time: [Int]?         
    var time: [Int]?         // 本地推送当天时间（24小时制）
    //: var msg: [String]?       
    var msg: [String]?       // 本地推送文案
}

//: struct UserInfoModel: HandyJSON {
struct SchemeLoopDevice: HandyJSON {
    //: var headPic: String?   
    var headPic: String?   // 头像
    //: var nickname: String?  
    var nickname: String?  // 昵称
    //: var uid: String?       
    var uid: String?       // 用户Id
}

//: extension AppWebViewController {
extension ScreenPast {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func telephoneMessageThird(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = FlagSort.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case viewOpengoingPackageMigrationSecret:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = KeyPrepare.past()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: notiNetValue.map{offceMore(agent: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case parserMicIndexPath:
                //: AppWebViewController.getFireBaseToken { str in
                ScreenPast.balefire { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: kCaptureMode.map{ofTowardSelect(format: $0)}, encoding: .utf8)!: str])
                }
                
            //: case getAreaISO:
            case main_partToLogUrl:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = ScreenPast.bushidoInfo()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_fireBehaviorResult.reversed(), encoding: .utf8)!: arr.joined(separator: ",")])
                
            //: case getProxyStatus:
            case engineInstallValue:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = ScreenPast.visibleColor()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_rateDict.map{productionDisplay(confirm: $0)}, encoding: .utf8)!: status])
              
            //: case getLangCode:
            case engineRandomMessage:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: modelDecideMessage.map{timeServer(stop: $0)}, encoding: .utf8)!: langCode])
                
            //: case getTimeZone:
            case engineCanulateName:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: dataDigitiserID.map{administrativeDivision(global: $0)}, encoding: .utf8)!: timeZone])
                
            //: case getInstalledApps:
            case viewExceptionVersion:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: loggerYourValue.reversed(), encoding: .utf8)!: apps])
                
            //: case getSystemUUID:
            case appLocalVersion:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: showBarrelStatus.map{$0^48}, encoding: .utf8)!: uuid])
                
            //: case getCountryCode:
            case configPlacePushMode:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: sessionZoneApplicationValue.map{$0^88}, encoding: .utf8)!: countryCode])
                
            //: case inAppRating:
            case cacheDoinggoingStr:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                ScreenPast.policy()

            //: case apPay:
            case data_toMsg:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.pome(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: appHeadFlag.map{sumCenter(flexible: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case loggerLocalValue:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.pome(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: appHeadFlag.map{sumCenter(flexible: $0)}, encoding: .utf8)!: success])
                    }
                }
                
            //: case openSystemBrowser:
            case cacheSphereName:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }
                
            //: case closeWebview:
            case kOutputPath:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.wattleGo()
                
            //: case openNewWebview:
            case main_levelId:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                    //: let transparency = model.transparency,
                    let transparency = model.transparency,
                    //: let fullscreen = model.fullscreen {
                    let fullscreen = model.fullscreen {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    ScreenPast.rootTo(urlStr, transparency, fullscreen)
                }
                
            //: case reloadWebview:
            case cacheServerMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.vaneUser()
            
            //: case openSettings:
            case main_installTitle:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: if let url = URL(string: UIApplication.openSettingsURLString) {
                if let url = URL(string: UIApplication.openSettingsURLString) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                }
                
            //: case setScheduledLocalPush:
            case main_photoFlag:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: LocalPushScheduler.shared.schedule(times: model.time ?? [], contents: model.msg ?? [])
                EnvironmentArrow.shared.toAfter(times: model.time ?? [], contents: model.msg ?? [])
                
            //: case getNotificationStatus:
            case modelResourceFormat:
                //: AppPermissionTool.shared.requestNotificationPermission { auth, isFirst in
                PendingAttribute.shared.ratingOff { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_privacyColorTowardName.map{$0^71}, encoding: .utf8)!: auth, String(bytes: networkDisplayMessage.map{errorFailure(product: $0)}, encoding: .utf8)!: isFirst])
                }
            
            //: case getMicStatus:
            case app_trustStuffState:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                PendingAttribute.shared.device { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_privacyColorTowardName.map{$0^71}, encoding: .utf8)!: auth, String(bytes: networkDisplayMessage.map{errorFailure(product: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getPhotoStatus:
            case showExceptionStr:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                PendingAttribute.shared.withModel { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_privacyColorTowardName.map{$0^71}, encoding: .utf8)!: auth, String(bytes: networkDisplayMessage.map{errorFailure(product: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getCameraStatus:
            case data_countMsg:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                PendingAttribute.shared.request { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_privacyColorTowardName.map{$0^71}, encoding: .utf8)!: auth, String(bytes: networkDisplayMessage.map{errorFailure(product: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case reportAdjust:
            case noti_viewMsg:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        KeyPrepare.getThroughKind(token: token, count: count)
                    //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        KeyPrepare.button(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case modelAppValue:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: modelDayStatus.map{coreGroup(version: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                ScreenPast.roundUsed(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event
//: extension AppWebViewController {
extension ScreenPast {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func rootTo(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = ScreenPast()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        ForceDomain.fromFrame()?.present(vc, animated: true)
    }
    
    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func roundUsed(_ model: FlagSort) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()
                
            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(givenTextStr)" + (String(noti_arrayPath.prefix(6)) + "lPush")
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: networkErrorKey.map{goOriginal(finish: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }
                
            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }
    
    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func balefire(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
            //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func bushidoInfo() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func visibleColor() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if ScreenPast.firstScript() || ScreenPast.timing() {
            //: return true
            return true
        }
        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func firstScript() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(routerEnterSecret) + noti_discourtesyActivityName.replacingOccurrences(of: "photo", with: "y"))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(sessionBasisState.replacingOccurrences(of: "control", with: "TT") + String(modelMakeStr))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(managerSubGlobalDate.replacingOccurrences(of: "presentation", with: "C") + "Proxy")] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理VPN
   /// - Returns: false: 未开启  true: 开启
   //: class func isUsedVPN() -> Bool {
   class func timing() -> Bool {
       //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       //: guard let dict = proxy as? [String: Any] else { return false }
       guard let dict = proxy as? [String: Any] else { return false }
       //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
       guard let scopedDic = dict[(String(kGrantStatusSecret))] as? [String: Any] else { return false }
       //: for keyStr in scopedDic.keys {
       for keyStr in scopedDic.keys {
           //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
           if keyStr.contains((const_showPath.replacingOccurrences(of: "origin", with: "a"))) || keyStr.contains((String(serviceSearchData))) || keyStr.contains((sessionStatusMigrationStr.replacingOccurrences(of: "file", with: "p"))) || keyStr.contains((cacheArrayStatus.replacingOccurrences(of: "build", with: "pp"))){
               //: return true
               return true
           }
       }
       //: return false
       return false
   }
    
    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func policy() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
        //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }
    
    // MARK: - Event
    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func pome(productId: String, source: Int = -1, payType: CoordinatorScreen, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        FollowView.range()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        PastForceContainer.shared.pad(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            FollowView.filterBack()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    FollowView.title( (String(constContactWhiteMessage.prefix(5)) + String(kTransformURL)) + "\"" + (String(appHourMessage) + dataThirdList.replacingOccurrences(of: "package", with: "ba")) + "\"" + (String(kStateFrameValue) + String(sessionTitleUrl.suffix(4))))
                    
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.pic()
                    
                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}